// CHARACTER SWITCH -- COMPONENT
// ------------------------------

import React, { Component } from 'react'

import Character from "./Character"

export default class CharacterSwitch extends Component {
    render() {
        return (
            <div>
                test
                {/* map through the component Characters so that  */}
            </div>
        )
    }
}
