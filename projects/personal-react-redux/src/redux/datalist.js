// DATALIST REDUCER




let datalist = (prevDatalist = [], action) => {
    switch (action.type) {
        case "POPULATE_VIDEOS":
            return [...prevDatalist, ]
        default:
            return prevDatalist
    }
}